package com.example.usermanagmentservice.dto;

public enum Status {
    IN_PROCESS,  // an in-process status
    CANCELLED,         // Represents a cancelled status
    SUCCESSFULLY_BOOKED, // Represents a successfully booked status
    FINISHED // Represents a finished status
}
